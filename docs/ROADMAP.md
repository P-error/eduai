# Roadmap (v2)

Priority order is based on thesis defensibility and data integrity.

1. Tighten response-format taxonomy/runtime alignment (either remove non-mcq tags from UX taxonomy or implement actual non-mcq runner support).
2. Strengthen compliance interpretability with per-question evidence snapshots for audit.
3. Add explicit retention/deletion controls for chat and attempt metadata.
4. Add controlled A/B experiment bookkeeping (session-level assignment IDs).
5. Expand quality checks for generated questions (duplicate detection, trivial distractor detection).
6. Improve admin trend chart UX (current table/bar blocks are dense but basic).
7. Add exportable analysis bundles for thesis appendix (filtered aggregates + metadata dictionary).
