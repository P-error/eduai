This directory is the fixed runtime-slot handoff target for an EduAI-native pedagogy artifact package.

Current contract:
- the slot may contain a packaged artifact plus support files;
- website runtime serving remains inactive unless explicitly enabled later;
- synthetic internal artifacts must be labeled as bridge-stage evidence only;
- the external ASSISTments bootstrap winner is intentionally not loaded here.
