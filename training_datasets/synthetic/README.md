Synthetic-phase training snapshots are written here.
The first intended use is the EduAI-native synthetic internal bridge stage.
Snapshot `metadata.json` records exported row counts, sequence-role coverage, and dataset-origin coverage for internal auditability.
These snapshots are internal synthetic evidence only and do not activate learner-facing runtime serving by themselves.
