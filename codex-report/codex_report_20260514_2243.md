# Codex report 20260514_2243

## Task summary

Finalize THU ML scorer productization for EduAI behind controlled env flags, with runtime artifact placement, rollback documentation, safety checks, final route smoke, and report delivery.

## What was changed

- Added a versioned runtime copy of the THU scorer artifact outside the `current` pointer.
- Added a runtime manifest with SHA-256, model version, source lane, and activation note.
- Added non-finite scorer output protection so NaN/Inf model output falls back through the existing explicit fallback path.
- Documented THU runtime deployment and updated apply-mode documentation.
- Ran final `POST /api/chat` shadow/apply smoke via local Next.js and mock OpenAI-compatible endpoint.

## Changed files

- `src/lib/ml-six-factor-runtime-scorer.ts`
- `src/lib/ml-six-factor-shadow-self-check.ts`
- `docs/ml_six_factor_apply_mode.md`
- `ml/src/eduai_ml/training/THU/ML_RUNTIME_DEPLOYMENT.md`
- `ml/src/eduai_ml/training/THU/codex_final_ml_productization_report_20260514_2243.md`
- `artifacts/runtime/eduai_native_pedagogy/thu_linear_candidate_scorer_v1/artifact.json`
- `artifacts/runtime/eduai_native_pedagogy/thu_linear_candidate_scorer_v1/runtime_manifest.json`
- `codex-report/codex_report_20260514_2243.md`

## Key touched modules/contracts

- `scoreSixFactorCandidate`
- `resolveSixFactorPolicyDecisionForFeatures`
- `sixFactorShadow`
- `sixFactorDeliveredConfig`
- `POST /api/chat`
- THU runtime artifact handoff contract

## Checks/scripts run

- `npx prisma validate`
- `EDUAI_SIX_FACTOR_ARTIFACT_PATH=artifacts/runtime/eduai_native_pedagogy/thu_linear_candidate_scorer_v1/artifact.json bash scripts/ml-six-factor-shadow-self-check.sh`
- `jq empty` for runtime artifact and manifest
- `sha256sum` for training/runtime artifact parity
- `git diff --check`
- `POST /api/chat` shadow route smoke
- `POST /api/chat` apply route smoke

## Passed/failed checks

All checks passed.

## Behavioral change

THU scorer can now be used from `artifacts/runtime/eduai_native_pedagogy/thu_linear_candidate_scorer_v1/artifact.json` under existing env flags. Non-finite scorer output now falls back explicitly with `scoring_error`.

## Data/model honesty note

Actually implemented: JSON `linear_candidate_scorer_v1_seed_42` trained on synthetic THU observations and served through the six-factor policy adapter. Not trained/proven: real-user efficacy and calibration. Remaining bridge logic: heuristic/static fallback, guardrails, and render mapping.

## Intentionally not done

No production env activation, no `current` pointer overwrite, no UI change, no schema migration, no full counterfactual ranking, no commit/push.

## Weakest remaining point

The model is synthetic-trained; real-user educational efficacy remains unproven.

## Next-step impact

This unlocks controlled local/demo/production shadow rollout and active apply demo with one-variable learner-facing rollback.

## Important remaining limitations

Route smoke used a mock LLM endpoint and temporary local Postgres startup; smoke records were cleaned up and the container was stopped.

## Report delivery status to phone

Failed. Reason: Taildrop to `100.88.154.9` failed because the peer is offline.
