# Local development environment template.
# Do not put real credentials in this file.

# PostgreSQL.
# Use the pooled/runtime connection for DATABASE_URL and the direct/unpooled
# connection for DIRECT_URL when using Neon.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require&channel_binding=require"
DIRECT_URL="postgresql://USER:PASSWORD@DIRECT_HOST/DB?sslmode=require&channel_binding=require"

# JWT signing secret for local auth
JWT_SECRET="replace-with-a-long-random-local-secret"

# OpenAI-compatible provider
OPENAI_API_KEY="replace-with-local-provider-key"
OPENAI_BASE_URL="https://api.openai.com/v1"
# Optional semantic validation for generated tests. Disabled by default.
EDUAI_LLM_TEST_JUDGE="0"

# Privacy default: keep raw chat storage disabled
CHAT_STORE_RAW_CONTENT="0"

# Optional: dataset export pseudonym secret
DATASET_EXPORT_SECRET="replace-with-local-export-secret"

# Local/demo six-factor primary pedagogical decision runtime is explicit-on.
# EDUAI_SIX_FACTOR_SHADOW="1" builds metadata/logging for six-factor decisions.
# EDUAI_SIX_FACTOR_ML_POLICY="1" enables artifact candidate scoring for the six-factor pedagogical decision.
# EDUAI_SIX_FACTOR_APPLY="1" applies the selected six-factor profile to learner-facing prompts.
# EDUAI_SIX_FACTOR_SHADOW_ONLY="1" means scoring/logging without learner-facing apply.
# EDUAI_SIX_FACTOR_ARTIFACT_PATH is separate from configs/active_policy.json.
EDUAI_SIX_FACTOR_SHADOW="1"
EDUAI_SIX_FACTOR_ML_POLICY="1"
EDUAI_SIX_FACTOR_APPLY="1"
EDUAI_SIX_FACTOR_SHADOW_ONLY="0"
EDUAI_SIX_FACTOR_ARTIFACT_PATH="artifacts/runtime/eduai_native_pedagogy/catboost_candidate_scorer_v1/artifact.json"

EDUAI_CATBOOST_SCORER_MODE="python_cli"
EDUAI_CATBOOST_PYTHON_BIN="ml/.venv/bin/python"
EDUAI_CATBOOST_SCORER_SCRIPT="ml/scripts/score_catboost_candidate_scorer.py"
EDUAI_CATBOOST_SCORER_TIMEOUT_MS="10000"

# Legacy prediction accuracy/time ML-first requires a runtime-eligible trained artifact and active_policy artifact_ml.
# The default local path is intentionally ignored unless created by training/export.
# Do not use synthetic or unfiltered local artifacts as ML-first readiness evidence.
# Forced DEV pipeline checks may use configs/ml_accuracy_logreg_artifact.dev.json, but productionEligible=false.
# EDUAI_ML_ARTIFACT_PATH="configs/ml_accuracy_logreg_artifact.local.json"
# UI visibility/debug flag only; does not enable or disable runtime personalization decisions.
NEXT_PUBLIC_SHOW_ML_PERSONALIZATION="1"
